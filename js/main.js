// Importer klassen "Nav" fra filen "nav.js"
import Nav from "./nav.js";

// Importer klassen "Feed" fra filen "feed.js"
import Feed from "./feed.js";

// Opprett en instans av klassen "Nav"
const nav = new Nav();

// Opprett en instans av klassen "Feed"
const feed = new Feed();
